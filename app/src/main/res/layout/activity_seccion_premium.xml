<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_light_black"
    android:padding="24dp">

    <!-- Botón atrás -->
    <ImageButton
        android:id="@+id/btn_back"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_back"
        app:tint="@color/text_white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <!-- Ícono corona premium -->
    <ImageView
        android:id="@+id/iv_premium_icon"
        android:layout_width="110dp"
        android:layout_height="110dp"
        android:layout_marginTop="48dp"
        android:src="@drawable/ic_crown"
        app:tint="#00BFA6"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Título -->
    <TextView
        android:id="@+id/tv_premium_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Premium Protection"
        android:textColor="#FFFFFF"
        android:textSize="30sp"
        android:textStyle="bold"
        android:layout_marginTop="24dp"
        app:layout_constraintTop_toBottomOf="@id/iv_premium_icon"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Subtítulo -->
    <TextView
        android:id="@+id/tv_premium_subtitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Desbloquea todas las funciones avanzadas"
        android:textColor="#00BFA6"
        android:textSize="16sp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toBottomOf="@id/tv_premium_title"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Tabla comparativa -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cv_benefits"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:cardBackgroundColor="@color/surface_grey"
        app:cardCornerRadius="20dp"
        app:layout_constraintTop_toBottomOf="@id/tv_premium_subtitle"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="40dp">

        <TableLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:stretchColumns="*"
            android:padding="20dp">

            <TableRow>
                <TextView android:text="Función" android:textStyle="bold" android:textColor="#FFFFFF" android:padding="14dp"/>
                <TextView android:text="Free" android:textStyle="bold" android:textColor="#FFFFFF" android:gravity="center" android:padding="14dp"/>
                <TextView android:text="Premium" android:textStyle="bold" android:textColor="#FFFFFF" android:gravity="center" android:padding="14dp"/>
            </TableRow>

            <!-- filas de comparación -->
            <TableRow>
                <TextView android:text="Compartir ubicación en tiempo real" android:textColor="#FFFFFF" android:padding="14dp"/>
                <ImageView android:src="@drawable/ic_cancel" app:tint="#FF5252" android:layout_width="32dp" android:layout_height="32dp" android:layout_gravity="center"/>
                <ImageView android:src="@drawable/ic_check" app:tint="#00BFA6" android:layout_width="32dp" android:layout_height="32dp" android:layout_gravity="center"/>
            </TableRow>

            <!-- ... agrega las otras filas que ya tenías ... -->

        </TableLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Botón para ver novedades / mejoras detalladas -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_view_novedades"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:text="Ver qué hay de nuevo en Premium"
        android:textAllCaps="false"
        android:textColor="#121212"
        android:textSize="18sp"
        android:textStyle="bold"
        app:backgroundTint="#00BFA6"
        app:cornerRadius="16dp"
        app:layout_constraintTop_toBottomOf="@id/cv_benefits"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp"/>

    <!-- Precio y trial -->
    <TextView
        android:id="@+id/tv_price"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="S/ 9.90 / mes"
        android:textColor="#FFFFFF"
        android:textSize="34sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/btn_view_novedades"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="32dp"/>

    <TextView
        android:id="@+id/tv_trial_note"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Prueba 7 días gratis • Cancela cuando quieras"
        android:textColor="#9DA4B0"
        android:textSize="14sp"
        app:layout_constraintBottom_toTopOf="@id/btn_activate_premium"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="16dp"/>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_activate_premium"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:text="Activar Premium ahora"
        android:textAllCaps="false"
        android:textColor="#121212"
        android:textSize="20sp"
        android:textStyle="bold"
        app:backgroundTint="#00BFA6"
        app:cornerRadius="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginBottom="40dp"/>

</androidx.constraintlayout.widget.ConstraintLayout>