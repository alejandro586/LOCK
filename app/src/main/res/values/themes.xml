<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base de la app (light) -->
    <style name="Theme.TuAppSeguridad" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores base (sobrescriben si no usas dynamic) -->
        <item name="colorPrimary">@color/turquesa</item>
        <item name="colorPrimaryVariant">@color/turquesa_dark</item>
        <item name="colorOnPrimary">@color/blanco</item>

        <item name="colorSecondary">@color/rojo</item>
        <item name="colorSecondaryVariant">@color/rojo_dark</item>
        <item name="colorOnSecondary">@color/blanco</item>

        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- Mejora contraste en status bar (lo que te pasé) -->
        <item name="android:enforceStatusBarContrast">true</item>

        <!-- Si usas diálogos flotantes (no recomendado para app full screen) -->
        <!-- <item name="android:windowIsFloating">false</item> -->  <!-- quítalo o ponlo solo en temas de dialog -->
    </style>
</resources>